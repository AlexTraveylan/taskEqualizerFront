export type ImageType = {
  src: string
  alt: string
  width: number
  height: number
}

export type NavigationItem = {
  label: Map<string, string>
  href: string
  ariaLabel: Map<string, string>
}
